{{>navBar}}
<main class="flex-container">
    <section class="col-10 flex-container">
        <div class="card my-5 mx-1">
            <div class="card-body">
                <h3 class="card-title text-center mb-4">{{ title }}</h3>
                <p class="text-center mb-1">Id carrito: <strong>{{ cartId }}</strong></p>
            </div>
        </div>
    </section>
</main>